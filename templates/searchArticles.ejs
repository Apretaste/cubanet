<div class = "row">
	<div class = "col s12 m12 xl12">
		<h1>Buscar: <% search %></h1>

		<% articles.forEach(function(article, i) { %>
			<small>
				<%= formatDate(article.pubDate) %>
			</small>
			<br/>
			<b>
				<a class="link link-simple" data-command = "CUBANET HISTORIA" data-query = "articles[<%= i %>].link">
                    <%= article.title %>
				</a>
			</b>
			<%= article.description.substr(0, 200) %><br/>
		<% }); %>

		<button class = "btn waves-effect waves-light" onclick = "apretaste.send({command:'CUBANET'})">
			Titulares
			<i class = "material-icons right">feed</i>
		</button>
	</div>
</div>